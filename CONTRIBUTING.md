# Contributing

For the hackathon submission, keep the required top-level template structure intact.

Place application source code inside `src/`. Do not commit real credentials or `.env` files.

Before pushing:
1. Build the program.
2. Run the demo.
3. Verify the generated output.
4. Check that no real credentials are committed.
5. Run the submission validator.
